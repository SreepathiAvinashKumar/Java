import java.util.Queue;
import java.util.LinkedList;

public class TheQueue {
    public static void main(String[] args){
    //     Queue<Person> supermarket = new LinkedList<>();
    //     supermarket.add(new Person("alex",15));
    //     supermarket.add(new Person("ak",33));
    //     supermarket.add(new Person("kalyan",33));
    //     System.out.println(supermarket.peek());
    //     // System.out.println(supermarket.poll()); // used to remove the first element from queue 
    //     System.out.println(supermarket.size());
        
    // }

    // static record Person(String name,int age){}



    // Queue<Integer> numbers = new LinkedList<>();
    // numbers.add(1);
    // numbers.add(3);
    // numbers.add(4);

    // System.out.println(numbers.peek());   // It returns the first element in queue but top element in stack
    // System.out.println(numbers.size());
    // System.out.println(numbers.poll());  // It returns the first element and removes that element        It returns null if queue is empty
    // System.out.println(numbers.size());
    

    }
}